# KLShareLink 아키텍처

> <span lang="ko">[English](../../README.md) · [简体中文](../zh-Hans/README.md) · [繁體中文](../zh-Hant/README.md) · [日本語](../ja/README.md) · [한국어](../ko/README.md)</span>

## 개요

분석기는 신뢰할 수 없는 공유 내용과 앱의 화면 이동 또는 네트워크 처리 사이에 둡니다. 명시 URL 검증, Foundation을 이용한 후보 검색, scheme과 host 검사, 서비스 대조, 이전 형식의 식별 문자열 확인 순으로 처리합니다. 판정 규칙은 설정값으로 전달하고 네트워크 요청 같은 부수 효과는 앱에서 수행합니다.

## 동작 보장

- 필터를 통과한 명시적 URL을 가장 먼저 사용
- 본문의 HTTP(S) URL을 등장 순서대로 검색
- 설정한 서비스 도메인과 일치하는 URL을 우선 선택
- `localhost`, `.local`, `::1`, 문서에 명시된 직접 입력 IPv4 범위 거부
- URL이 없어도 이전 형식의 식별 문자열에서 서비스 이름을 확인 가능

## 책임 경계

이 패키지는 링크 선택만 담당합니다. 네트워크 요청, 리디렉션 추적, 추적 파라미터 제거, 방문 기록 저장은 수행하지 않습니다. `::1` 이외의 로컬 또는 사설 IPv6 리터럴은 필터에서 거부하지 않습니다. 네트워크 계층에서 해석된 주소와 모든 리디렉션 목적지를 검증해야 합니다.

이 API는 계산 결과만 반환하며 앱의 상태를 변경하지 않습니다. 입력, 설정, 시스템 프레임워크의 동작이 같으면 동일한 결과를 반환합니다.
