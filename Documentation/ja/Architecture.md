# KLShareLink アーキテクチャ

> <span lang="ja">[English](../../README.md) · [简体中文](../zh-Hans/README.md) · [繁體中文](../zh-Hant/README.md) · [日本語](../ja/README.md) · [한국어](../ko/README.md)</span>

## 概要

解析器は、信頼できない共有内容とアプリの画面遷移または通信処理の間に置きます。明示 URL の検証、Foundation による候補の検出、スキームとホストの検査、サービスとの照合、旧形式の識別文字列の確認という順で処理します。判定ルールは設定値として渡し、通信などの副作用はアプリ側で実行します。

## 動作保証

- フィルターを通過した明示 URL を最優先
- 文章中の HTTP(S) URL を出現順に検出
- 設定したサービスのドメインに一致する URL を優先
- `localhost`、`.local`、`::1`、および文書化された直接指定の IPv4 範囲を拒否
- URL が見つからない場合も、旧形式の識別文字列からサービス名だけを返せる

## 責務の境界

このパッケージはリンクの選択だけを行います。通信、リダイレクト先の展開、トラッキングパラメータの削除、履歴保存は行いません。`::1` 以外のローカルまたはプライベートな IPv6 リテラルはフィルターで拒否されません。解決後のアドレスと各リダイレクト先はネットワーク層で検証してください。

この API は計算結果だけを返し、アプリ側の状態は変更しません。入力、設定、システムフレームワークの挙動が同じであれば、同じ結果を返します。
